#include <bits/stdc++.h>
using namespace std;
int main() {
    unordered_map<int,int>dee;
    vector<int>v={1,1,1,2,3,4,3,4,5,6,7,7};
    for(int i=0;i<v.size();i++){
        dee[v[i]]+=1;
    }
    for(auto it:dee){
        cout<<it.first<<" "<<it.second<<endl;
    }
    return 0;
}
